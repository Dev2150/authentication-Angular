// for interfaces/types throughout the application
export type RegisterForm = {
    email: string
    password: string
    firstName: string
    lastName: string
    country: string
}

export type LoginForm = {
    email: string
    password: string
}